<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Job Status Tree View -->
    <record id="view_job_status_tree" model="ir.ui.view">
        <field name="name">job.status.tree</field>
        <field name="model">job.status</field>
        <field name="arch" type="xml">
            <tree editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <!-- Job Status Form View -->
    <record id="view_job_status_form" model="ir.ui.view">
        <field name="name">job.status.form</field>
        <field name="model">job.status</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="sequence"/>
                        </group>
                        <group>
                            <field name="active"/>
                            <field name="color"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Job Status Action -->
    <record id="action_job_status" model="ir.actions.act_window">
        <field name="name">Job Status</field>
        <field name="res_model">job.status</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first job status!
            </p>
            <p>
                Job statuses are used to track the progress of daily tasks and activities.
            </p>
        </field>
    </record>
</odoo>